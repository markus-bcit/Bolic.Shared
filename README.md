# Bolic.Shared

Shared library for the Bolic backend.

A GitHub PAT with `read:packages` scope is required.

## Versioning

| Pattern | Channel |
|---|---|
| `1.0.x-dev` | Prerelease (develop branch) |

No deployment for main/prod packages yet
